spring:
  cloud:
    config:
      # 该名称要和Github .yml 前缀名称一致
      name: spring-cloud-client
      # 本次访问的配置项
      profile: dev
      label: master
      # 本为服务启动先去找3344号微服务（spring cloud config server），
      # 通过Spring Cloud Config 获取Github的服务地址
      uri: htt://localhost:3344
